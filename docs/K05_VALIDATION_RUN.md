# K0.5 validation run

This commit exists to trigger the predeclared K0.5 GitHub Actions validation workflow after the workflow itself became part of the default branch.

No validation thresholds or synthesis logic are changed here.
