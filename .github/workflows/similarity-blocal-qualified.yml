name: SIMILARITY flow-qualified prospective B-local ASIC study

env:
  ORFS_IMAGE: openroad/orfs@sha256:573c1716efa0e286c4f641c26d343e20929d58d27fffbb22be2d0b93f09764f6

on:
  pull_request:
    paths:
      - 'asic/blocal_qualified/**'
      - 'docs/SIMILARITY_BLOCAL_QUALIFIED_PREREGISTRATION.md'
      - 'scripts/similarity_blocal_qualified_*.py'
      - 'scripts/similarity_antenna_margin_canary.py'
      - 'scripts/similarity_asic_transfer_route.py'
      - 'rtl/kernellum_blocal_mac_array.sv'
      - 'rtl/similarity_asic_transfer_blocal.sv'
      - 'rtl/tb_kernellum_blocal_equivalence.sv'
      - 'tests/test_similarity_blocal_qualified.py'
      - '.github/workflows/similarity-blocal-qualified.yml'
  push:
    branches: [main]
    paths:
      - 'asic/blocal_qualified/**'
      - 'docs/SIMILARITY_BLOCAL_QUALIFIED_PREREGISTRATION.md'
      - 'scripts/similarity_blocal_qualified_*.py'
      - 'scripts/similarity_antenna_margin_canary.py'
      - 'scripts/similarity_asic_transfer_route.py'
      - 'rtl/kernellum_blocal_mac_array.sv'
      - 'rtl/similarity_asic_transfer_blocal.sv'
      - 'rtl/tb_kernellum_blocal_equivalence.sv'
      - 'tests/test_similarity_blocal_qualified.py'
      - '.github/workflows/similarity-blocal-qualified.yml'

permissions:
  contents: read

jobs:
  preflight:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with: {python-version: '3.12'}
      - name: Verify frozen source, flow and protocol
        run: |
          python -m pip install pytest
          PYTHONPATH=. python -m pytest -q tests/test_similarity_blocal_qualified.py
          python -m py_compile \
            scripts/similarity_blocal_qualified_route.py \
            scripts/similarity_blocal_qualified_validate.py
          grep -q 'Status: \*\*draft until merged to main; no eligible physical route may precede that merge\*\*' \
            docs/SIMILARITY_BLOCAL_QUALIFIED_PREREGISTRATION.md
          echo '7cdd3223ac401ec9c9df5a792cb41f2e754262dbd2c6fad103752fce7afdc766  rtl/kernellum_blocal_mac_array.sv' | sha256sum -c -
          echo '1f6bd084dab5e368f34dee5c44fb6ca12a68860b4b26fff2f78b85277d50552a  rtl/similarity_asic_transfer_blocal.sv' | sha256sum -c -
          for family in blocal controls; do
            for platform in nangate45 sky130hd; do
              grep -qx 'export CAP_MARGIN = 30' "asic/blocal_qualified/${family}_config_${platform}.mk"
              grep -qx 'export SLEW_MARGIN = 25' "asic/blocal_qualified/${family}_config_${platform}.mk"
            done
          done
          grep -q 'RATIO_MARGIN = 20' scripts/similarity_blocal_qualified_route.py
      - name: Compare signed GEMM on all four unopened shapes
        run: |
          sudo apt-get update
          sudo apt-get install -y iverilog
          mkdir -p results/similarity_blocal_qualified_preflight
          for geometry in '6 10' '10 6' '7 10' '10 7'; do
            set -- $geometry
            iverilog -g2012 -Wall -DTEST_ROWS=$1 -DTEST_COLS=$2 \
              -s tb_kernellum_blocal_equivalence \
              -o "results/similarity_blocal_qualified_preflight/tb_${1}x${2}.vvp" \
              rtl/kernellum_local_mac_array.sv \
              rtl/kernellum_blocal_mac_array.sv \
              rtl/tb_kernellum_blocal_equivalence.sv
            vvp "results/similarity_blocal_qualified_preflight/tb_${1}x${2}.vvp" | \
              tee "results/similarity_blocal_qualified_preflight/tb_${1}x${2}.log"
            grep -q "KERNELLUM_BLOCAL_EQUIVALENCE_PASS rows=$1 cols=$2" \
              "results/similarity_blocal_qualified_preflight/tb_${1}x${2}.log"
          done
          touch results/similarity_blocal_qualified_preflight/similarity_blocal_qualified_functional_passed
          git rev-parse HEAD > results/similarity_blocal_qualified_preflight/source_sha.txt
          sha256sum \
            docs/SIMILARITY_BLOCAL_QUALIFIED_PREREGISTRATION.md \
            scripts/similarity_blocal_qualified_route.py \
            scripts/similarity_blocal_qualified_validate.py \
            scripts/similarity_antenna_margin_canary.py \
            rtl/kernellum_blocal_mac_array.sv \
            rtl/similarity_asic_transfer_blocal.sv \
            asic/blocal_qualified/*.mk \
            > results/similarity_blocal_qualified_preflight/source_hashes.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: similarity-blocal-qualified-functional
          path: results/similarity_blocal_qualified_preflight/*
          retention-days: 90

  route:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && startsWith(github.event.head_commit.message, 'Freeze flow-qualified prospective B-local study')
    needs: preflight
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    strategy:
      fail-fast: false
      max-parallel: 12
      matrix:
        platform: [nangate45, sky130hd]
        topology: [broadcast, local, blocal]
        seed: [293, 317, 347]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with: {python-version: '3.12'}
      - name: Prepare pinned OpenROAD flow
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-user-static
          docker pull "$ORFS_IMAGE"
          mkdir -p "$RUNNER_TEMP/orfs"
          cid="$(docker create "$ORFS_IMAGE")"
          docker cp "$cid:/OpenROAD-flow-scripts/flow" "$RUNNER_TEMP/orfs/"
          docker rm "$cid"
          test -f "$RUNNER_TEMP/orfs/flow/scripts/final_outputs.tcl"
          test -f "$RUNNER_TEMP/orfs/flow/scripts/global_route.tcl"
      - name: Apply frozen repairs and route four unopened shapes
        run: |
          PYTHONPATH=. python scripts/similarity_blocal_qualified_route.py \
            --platform ${{ matrix.platform }} \
            --topology ${{ matrix.topology }} \
            --seed ${{ matrix.seed }} \
            --flow-root "$RUNNER_TEMP/orfs/flow"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: similarity-blocal-qualified-route-${{ matrix.platform }}-${{ matrix.topology }}-s${{ matrix.seed }}
          path: |
            results/similarity_blocal_qualified/similarity_blocal_qualified_${{ matrix.platform }}_${{ matrix.topology }}_s${{ matrix.seed }}.csv
            results/similarity_blocal_qualified/flow_patch_${{ matrix.platform }}_${{ matrix.topology }}_s${{ matrix.seed }}.json
            build/similarity_blocal_qualified/${{ matrix.platform }}/${{ matrix.topology }}/s${{ matrix.seed }}/**/reports/*
            build/similarity_blocal_qualified/${{ matrix.platform }}/${{ matrix.topology }}/s${{ matrix.seed }}/**/logs/*.log
            build/similarity_blocal_qualified/${{ matrix.platform }}/${{ matrix.topology }}/s${{ matrix.seed }}/**/driver.log
          retention-days: 90

  validate:
    if: always() && github.event_name == 'push' && github.ref == 'refs/heads/main' && startsWith(github.event.head_commit.message, 'Freeze flow-qualified prospective B-local study')
    needs: [preflight, route]
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with: {python-version: '3.12'}
      - uses: actions/download-artifact@v5
        with:
          pattern: similarity-blocal-qualified-*
          path: results/similarity_blocal_qualified_download
      - name: Apply every frozen all-required gate
        run: |
          PYTHONPATH=. python scripts/similarity_blocal_qualified_validate.py \
            --input results/similarity_blocal_qualified_download
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: similarity-blocal-qualified-final-summary
          path: results/similarity_blocal_qualified_summary.json
          retention-days: 90
